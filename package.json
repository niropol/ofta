{
  "name": "sam-app",
  "version": "0.0.0",
  "private": true,
  "description": "SAM Oftalmología — sistema de gestión (tests con vitest + jsdom)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^24.1.0",
    "vitest": "^2.0.5"
  }
}
